export default function Portal() {
  return (
    <main className="mx-auto max-w-5xl p-6 space-y-4">
      <h1 className="text-3xl font-bold">ABCEMS Portal</h1>
      <p>Dashboard, People, Training, Sessions, Reports — pay, homework, roster CSV, certificates, audit pack.</p>
      <p><strong>TODO: OPEN QUESTION</strong> — Add NextAuth gating when email provider is ready.</p>
    </main>
  );
}
